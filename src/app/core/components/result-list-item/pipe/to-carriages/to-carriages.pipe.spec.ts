import { ToCarriagesPipe } from './to-carriages.pipe';

describe('ToCarriagesPipe', () => {
    it('create an instance', () => {
        const pipe = new ToCarriagesPipe();
        expect(pipe).toBeTruthy();
    });
});
