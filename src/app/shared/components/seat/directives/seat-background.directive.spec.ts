import { SeatBackgroundDirective } from './seat-background.directive';

describe('SeatBackgroundDirective', () => {
    it('should create an instance', () => {
        const directive = new SeatBackgroundDirective();
        expect(directive).toBeTruthy();
    });
});
