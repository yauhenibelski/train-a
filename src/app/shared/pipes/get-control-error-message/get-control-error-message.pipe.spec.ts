import { GetControlErrorMessagePipe } from './get-control-error-message.pipe';

describe('GetControlErrorMessagePipe', () => {
    it('create an instance', () => {
        const pipe = new GetControlErrorMessagePipe();
        expect(pipe).toBeTruthy();
    });
});
