import { ToDateTabsPipe } from './to-date-tabs.pipe';

describe('ToDateTabsPipe', () => {
    it('create an instance', () => {
        const pipe = new ToDateTabsPipe();
        expect(pipe).toBeTruthy();
    });
});
