import { TimeArrivalPipe } from './time-arrival.pipe';

describe('TimeArrivalPipe', () => {
    it('create an instance', () => {
        const pipe = new TimeArrivalPipe();
        expect(pipe).toBeTruthy();
    });
});
