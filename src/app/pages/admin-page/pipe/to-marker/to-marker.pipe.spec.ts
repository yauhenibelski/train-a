import { ToMarkerPipe } from './to-marker.pipe';

describe('ToMarkerPipe', () => {
    it('create an instance', () => {
        const pipe = new ToMarkerPipe();
        expect(pipe).toBeTruthy();
    });
});
