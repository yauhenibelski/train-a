import { GetConnectedCityPipe } from './get-connected-city.pipe';

describe('GetConnectedCityPipe', () => {
    it('create an instance', () => {
        const pipe = new GetConnectedCityPipe();
        expect(pipe).toBeTruthy();
    });
});
